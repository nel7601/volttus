# Neon PostgreSQL (direct connection, not pooled)
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# Same key used in Vercel for encrypting Emporia passwords
EMPORIA_ENCRYPTION_KEY=generate-with-openssl-rand-hex-32

# Polling interval
POLL_INTERVAL_MINUTES=15

LOG_LEVEL=INFO
